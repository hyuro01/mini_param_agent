# mini_param_agent Configuration Example
# 
# Configuration File Locations (in priority order):
# 1) mini_param_agent/config/config.yaml - Development mode (current directory)
# 2) ~/.mini_param_agent/config/config.yaml - User config directory
# 3) <package>/mini_param_agent/config/config.yaml - Package installation directory
#
# To use this config:
# - Copy this file to one of the above locations as config.yaml
# - Fill in your API key and customize settings as needed
# - All config files (config.yaml, mcp.json, system_prompt.md) are in the same directory

api_key: Your API Key
api_base: Your API Base
model: Your Model
# LLM provider: "anthropic" or "openai"
provider: "anthropic"

# ===== Retry Configuration =====
retry:
  enabled: true           # Enable retry mechanism
  max_retries: 3          # Maximum number of retries
  initial_delay: 1.0      # Initial delay time (seconds)
  max_delay: 60.0         # Maximum delay time (seconds)
  exponential_base: 2.0   # Exponential backoff base (delay = initial_delay * base^attempt)

# ===== Agent Configuration =====
max_steps: 100  # Maximum execution steps

# Conversation memory. Match token_limit to the server's actual context window.
# Persistence is opt-in: snapshots contain private conversation and tool output.
context:
  token_limit: 80000
  reserve_tokens: 4096
  keep_recent_messages: 12  # Minimum; whole user rounds remain together
  strategy: summary  # summary (LLM + bounded fallback) or recent (excerpts only)
  summary_chars: 4000
  summary_input_chars: 24000
  safety_margin: 1.15
  encoding: null  # Auto-select tiktoken encoding; unknown models use cl100k_base
  persistence: false
  storage_dir: .mini_param_agent/context  # Relative to workspace; absolute shared mount supported
  session_id: null  # Auto-generated; set a stable name to resume later
  resume: false  # Requires persistence: true and an existing session_id
  enable_recall: false  # Adds the read-only recall_context tool
workspace_dir: "./workspace"  # Working directory
system_prompt_path: "system_prompt.md"  # System prompt file (same config directory)

# ===== Tools Configuration =====
tools:
  # Basic tool switches
  enable_file_tools: true  # File read/write/edit tools (ReadTool, WriteTool, EditTool)
  enable_bash: true        # Bash command execution tool
  enable_note: true        # Session note tool (SessionNoteTool)
  enable_ml_experiment: true # MLExperimentTool: local training, Optuna search, and experiment reports
  
  # Claude Skills
  enable_skills: true      # Enable Skills
  skills_dir: "./skills"   # Skills directory path
  
  # MCP Tools
  enable_mcp: true         # Enable MCP tools
  mcp_config_path: "mcp.json"  # MCP configuration file (same config directory)
                           # Note: API Keys for MCP tools are configured in mcp.json
  # MCP timeout configuration (prevents hanging on network issues)
  mcp:
    connect_timeout: 10.0    # Connection timeout in seconds (default: 10)
    execute_timeout: 60.0    # Tool execution timeout in seconds (default: 60)
    sse_read_timeout: 120.0  # SSE read timeout in seconds (default: 120)
